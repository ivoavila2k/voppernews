CREATE TABLE news_sources (id UUID PRIMARY KEY DEFAULT gen_random_uuid(), name TEXT NOT NULL UNIQUE, country TEXT NOT NULL, feed_url TEXT NOT NULL, category TEXT NOT NULL DEFAULT 'Geral', active BOOLEAN NOT NULL DEFAULT TRUE, created_at TIMESTAMPTZ NOT NULL DEFAULT NOW())